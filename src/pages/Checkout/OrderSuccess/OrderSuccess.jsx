import React from 'react'
import styles from './OrderSuccess.module.scss'

const OrderSuccess = () => (
  <div className={ styles.OrderSuccess }>
    <h3>Successful order!</h3>
    <p>We will call you back in a few minutes</p>
  </div>
)

export default OrderSuccess
